version: '3.8'

services:
  english-word:
    build: .
    container_name: english-word-app
    ports:
      - "3010:3010"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3010
      - DB_PATH=/app/data/words.db
    restart: always
