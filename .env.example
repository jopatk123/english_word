# PORT、DB_PATH、JWT_SECRET、AI_SETTINGS_SECRET、API_TOKEN_PEPPER、ADMIN_JWT_SECRET、ADMIN_PASSWORD_HASH 均为必填。
# 留空会导致 Docker 部署失败或服务启动报错。

# 服务监听端口。
# Docker 部署示例：3010
PORT=3010

# SQLite 数据库文件路径。
# 相对路径相对于项目根目录，与启动时的工作目录无关。
# Docker 部署示例：/app/data/words.db
# 本地开发示例：./data/words.db
DB_PATH=./data/words.db

# JWT 签名密钥。
# 请替换为长度足够、不可预测的随机字符串。
JWT_SECRET=replace-with-a-long-random-secret

# AI 配置加密密钥。请使用与 JWT_SECRET 不同的随机字符串。
AI_SETTINGS_SECRET=replace-with-another-long-random-secret

# API Token 哈希 pepper。必须与 JWT_SECRET 使用不同的随机字符串。
# 用于 HMAC-SHA256 存储用户 API Token；改密或轮换 JWT_SECRET 不会撤销已创建的 API Token。
API_TOKEN_PEPPER=replace-with-another-long-random-secret

# 超级管理员 JWT 签名密钥。必须与 JWT_SECRET 使用不同随机字符串。
ADMIN_JWT_SECRET=replace-with-another-long-random-secret

# 超级管理员密码 bcrypt 哈希，用于登录 /super-admin。
# 生成示例：
# cd server && node -e "import bcrypt from 'bcryptjs'; console.log(await bcrypt.hash('your-admin-password', 12));"
# ️  Docker Compose .env 文件重要提示：bcrypt 哈希中每个 $ 字符需要转义为 $$
#    否则 Docker Compose 会将 $ 解释为变量插值，导致哈希损坏、登录失败。
#    示例：$2a$12$AbCd... 需写为 $$2a$$12$$AbCd...
#    服务端启动时会自动把 $$ 还原为 $，因此同一份 .env 既可用于 Docker Compose，
#    也可用于本地 dotenv 直连开发（两种写法都能被正确识别）。
ADMIN_PASSWORD_HASH=

# 允许跨域请求的来源白名单（逗号分隔，可选）。
# 未配置时禁止所有跨域请求（生产环境推荐留空，前后端同源部署）。
# 本地开发示例：ALLOWED_ORIGINS=http://localhost:5173
# ALLOWED_ORIGINS=

# 反向代理配置（可选）。
# 决定 Express 如何解析客户端真实 IP（影响登录/管理员登录/AI 接口的 IP 限流）。
# 未配置时：生产环境（NODE_ENV=production）默认信任一层反向代理（1），其他环境不信任（false）。
# 取值：false（服务直接暴露公网，不信任任何代理头）/ true / 正整数（信任的代理跳数）/ loopback / IP 网段。
# ⚠️  若服务直接暴露公网且未设置本项，客户端可伪造 X-Forwarded-For 绕过限流。
# TRUST_PROXY=1

# 可选：单词记忆图片目录。未设置时使用 DB_PATH 所在目录下的 uploads/
# Docker 示例：/app/data/uploads
# UPLOAD_DIR=
